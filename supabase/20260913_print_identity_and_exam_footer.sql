-- ============================================================================
-- Mr Center — هوية الطباعة + عبارة ختام ورقة الاختبار (2026-09-13)
-- شغّل بعد 20260913_exam_workspace.sql.
--
-- إعدادات الشعار والعلامة المائية والتذييل محفوظة عمداً في
-- center_settings.settings (JSONB)، وهي محمية بسياسات مالك السنتر القائمة.
-- يضيف هذا الترحيل فقط الحقل الدائم لعبارة نهاية كل اختبار ورقي.
-- ============================================================================

BEGIN;

ALTER TABLE public.app_exams
  ADD COLUMN IF NOT EXISTS paper_footer TEXT NOT NULL DEFAULT '';

COMMIT;
